version: '3.0'
services:
  db:
    restart: always
    image: postgres:alpine
    environment:
      - POSTGRES_PASSWORD=postgres
    volumes:
      - ./data:/var/lib/postgresql/data
    networks:
      - internal
  web:
    container_name: link
    restart: always
    depends_on:
      - db
    build: .
    env_file:
      - .env
    expose:
      - ${PORT}
    networks:
      - backend
      - internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.link.rule=Host(`link.paudel.me`)"
      - "traefik.http.routers.link.entrypoints=websecure"
      - "traefik.http.routers.link.tls.certresolver=certbot"

networks:
  backend:
    external: true
  internal:
    driver: bridge
